<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="onPageLoaded">
    <ActionBar title="User List" />
    <ListView items="{{ users }}">
        <ListView.itemTemplate>
            <Label class="user-list" text="{{ email }}" tap="onChatTap" />
        </ListView.itemTemplate>
    </ListView>
</Page>